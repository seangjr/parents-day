# Slice S08 — Admin console + shared-secret auth

Isolated git worktree on branch `slice/s08-admin`. Implement ONLY this slice.

## Read first
- `docs/SPEC.md` (admin), `docs/ROADMAP.md` (S08), `CONTEXT.md`.
- `docs/adr/0006-*` (shared-secret gate on `/admin` + `/led`; NOT Vercel Deployment Protection), `docs/adr/0004-*` (modes), `docs/adr/0005-*` (reset).
- Existing (import, do NOT modify): `lib/repo/index.ts` (`getRepo()`, `aggregates`, `familyByCode`, `reset`), `components/ui/*`.

## Build
- `middleware.ts` (project root) — gate `/admin` and `/led` (and their APIs) behind a shared secret from `ADMIN_SECRET` env (cookie or `?key=` → set cookie). Participant routes stay open. Use Vercel Routing Middleware conventions (Next 16).
- `app/admin/page.tsx` + client — controls: start/stop (event state flag), set LED mode (welcome/live/photo-moment/paused), force a reveal / trigger Photo Moment, view submissions + families (counts), remove/hide an item, reset all data (confirm).
- `app/api/admin/**` — route handlers backing those actions (mode set, remove item, reset, list) via the repo. All admin APIs also behind the secret.

## Constraints (STRICT)
- Touch ONLY: `middleware.ts`, `app/admin/**`, `app/api/admin/**`. Do NOT modify `lib/repo/*`, `app/led/*` (S07 owns), `package.json`, `app/layout.tsx`, `app/globals.css`.
- `bunx tsc --noEmit` green. No full build, no format, no new deps.
- Commit: `git add -A && git commit -m "feat(s08): admin console + shared-secret auth"`. One-line summary.
