# Slice S03 — Redis data layer (repository + fake)

You are an implementation agent in an **isolated git worktree** on branch
`slice/s03-data`. Implement ONLY this slice.

## Read first
- `docs/SPEC.md` → "Identity & family", "Architecture", "Privacy".
- `docs/adr/0002-identity-and-family-model.md`, `docs/adr/0001-serverless-vercel-redis-polling-client-first.md`, `docs/adr/0005-privacy-posture-internal-event.md`.
- `CONTEXT.md`, `lib/love-styles.ts` (import `LoveStyleId`).
- `@upstash/redis` is already installed.

## Build
Create `lib/repo/` with a repository **interface** + two implementations:
- `lib/repo/types.ts`: `Participant`, `Submission` (participantId, firstName, role, familyCode|null, primary: LoveStyleId, selfieUrl|null, ts), `Family` (code, name, memberIds[], createdTs), `Aggregates` (Record<LoveStyleId, number>, total).
- `lib/repo/repository.ts`: interface `Repository` with `recordSubmission(sub)` (idempotent by participantId — a retake replaces and keeps counters correct), `createFamily(name)` → `{ code }` (mint a confusion-safe code, no O/0/I/1), `joinFamily(code, participantId)` (enforce **cap 10**, returns confirm data or error), `familyByCode(code)`, `submissionsForFamily(code)`, `aggregates()`, `submissionLog(cursor)` (append-only, cursor-based for the LED), `reset()` (flush all).
- `lib/repo/memory.ts`: in-memory `Repository` (source of truth for tests).
- `lib/repo/upstash.ts`: Upstash Redis `Repository` (keys: `agg:<style>` counters, `family:<code>` hash, `sub:<id>`, `log` list). Read `UPSTASH_REDIS_REST_URL` / `_TOKEN` from env; construct lazily.
- `lib/repo/index.ts`: export a `getRepo()` that returns upstash when env is set, else memory (dev).

## Test (/tdd)
Create `lib/repo/repository.test.ts` (bun test) against the **memory** impl: idempotent retake keeps counters honest; createFamily → unique confusion-safe code; joinFamily enforces cap 10; aggregates reflect submissions; reset clears everything.

## Constraints (STRICT)
- Touch ONLY `lib/repo/**`. Do NOT modify `package.json`, `lib/love-styles.ts`, `lib/scoring*`, or anything else.
- Run `bunx tsc --noEmit` and `bun test lib/repo/repository.test.ts` — both green.
- Do NOT run the full build, do NOT format the repo, do NOT add dependencies.
- When green: `git add -A && git commit -m "feat(s03): redis repository + memory fake + tests"` in this worktree.
- Reply with a one-line summary.
