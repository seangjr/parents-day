# Slice S06 — Submit to server + community aggregation

Isolated git worktree on branch `slice/s06-submit`. Implement ONLY this slice.

## Read first
- `docs/SPEC.md` (architecture, client-first submit best-effort), `docs/ROADMAP.md` (S06), `CONTEXT.md`.
- `docs/adr/0001-*`, `docs/adr/0003-*`, `docs/adr/0005-*`.
- Existing (import, do NOT modify): `lib/repo/index.ts` (`getRepo()`, `recordSubmission`, `aggregates`), `lib/scoring.ts`, `lib/participant.ts`, `lib/love-styles.ts`.

## Build
- `app/api/submit/route.ts` — POST a submission {participantId, firstName, role, familyCode?, primary, selfieUrl?} → `getRepo().recordSubmission` (idempotent). Returns ok.
- `app/api/community/route.ts` — GET → `getRepo().aggregates()` (five counts + total, with percentages).
- `lib/submit-client.ts` — a best-effort `submitResult()` with retry/backoff (queue in memory; the personal result already shown client-side — submission is fire-and-forget-with-retry).
- Wire the S04 **result screen** ("Send to big screen" CTA) to call `submitResult()` — this is the ONE result-screen edit S06 owns.

## Constraints (STRICT)
- Touch ONLY: `app/api/submit/**`, `app/api/community/**`, `lib/submit-client.ts`, and the single result-screen file from S04 (to wire submit). Do NOT modify `lib/repo/*`, `lib/scoring*`, `app/family/**`, `app/api/family/**`, `package.json`, `app/layout.tsx`, `app/globals.css`.
- `bunx tsc --noEmit` green. No full build, no format, no new deps.
- Commit: `git add -A && git commit -m "feat(s06): submit + community aggregation"`. One-line summary.
