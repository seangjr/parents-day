# Slice S04 — Mobile quiz → client-first result

Isolated git worktree on branch `slice/s04-mobile`. Implement ONLY this slice.

## Read first
- `docs/SPEC.md` (mobile journey, client-first results), `docs/ROADMAP.md` (S04), `CONTEXT.md`.
- `docs/adr/0001-*` (client-first), `docs/adr/0003-*` (scoring).
- Existing (import, do NOT modify): `lib/scoring.ts` (`scoreQuiz`), `lib/love-styles.ts`, `components/ui/*`, `components/animation/*`, `lib/cn.ts`.

## Build
The participant mobile flow, result computed **on-device** (call `scoreQuiz` from `lib/scoring.ts`; never wait on a server for the result):
- `lib/participant.ts` — a client store (React context or hook + `localStorage`) holding an anonymous participant id (generated), first name, role, optional selfie (data URL, skippable), and the 5 answers. One participant per device (ADR-0002).
- `app/(experience)/` route group (or `app/start`, `app/quiz`, `app/result`):
  - Landing/start → profile (first name, role chips Parent/Child/Grandparent/Guardian/Other, optional skippable selfie).
  - 5-question quiz using `QuizOption` + `ProgressBar` (question copy from `docs/SPEC.md`/brief; answers A–E).
  - Result screen: compute `scoreQuiz(answers)`, render the Love Style with `LoveBadge` + a warm description + hybrid/"Rojak" display; CTAs "Send to big screen" and "View family mix" as links (targets built in later slices — link to `/family` and a submit handler stub that later slices wire).
- `lib/result-copy.ts` — the five Love Style descriptions (from `docs/SPEC.md` copy direction).

## Constraints (STRICT)
- Touch ONLY: `app/(experience)/**` (or `app/start`,`app/quiz`,`app/result`), `components/quiz/**`, `lib/participant.ts`, `lib/result-copy.ts`. Do NOT modify `lib/scoring*`, `lib/repo/*`, `components/ui/*`, `components/animation/*`, `package.json`, `app/layout.tsx`, `app/globals.css`.
- `bunx tsc --noEmit` green. Do NOT run the full build (Turbopack rejects the worktree node_modules symlink) or format. No new deps.
- Commit on this branch: `git add -A && git commit -m "feat(s04): mobile quiz + client-first result"`. One-line summary.
