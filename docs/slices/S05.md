# Slice S05 — Family create/join + Family Love Mix

Isolated git worktree on branch `slice/s05-family`. Implement ONLY this slice.

## Read first
- `docs/SPEC.md` (identity & family, family mix), `docs/ROADMAP.md` (S05), `CONTEXT.md`.
- `docs/adr/0002-*` (Family Code, cap 10, open join), `docs/adr/0003-*` (familyMix).
- Existing (import, do NOT modify): `lib/repo/index.ts` (`getRepo()`), `lib/scoring.ts` (`familyMix`), `lib/love-styles.ts`, `lib/participant.ts` (from S04 — read participant id/name/role), `components/ui/*`, `components/led/family-constellation.tsx` (reuse for the mix visual if useful).
- `qrcode` is installed for join-QR generation.

## Build
- `app/api/family/create/route.ts` — POST {name} → `getRepo().createFamily(name)` → `{ code }`.
- `app/api/family/join/route.ts` — POST {code, participantId} → `getRepo().joinFamily` (enforce cap 10; return confirm data `{ familyName }` or error).
- `app/api/family/[code]/route.ts` — GET → family members + `familyMix(members)` result.
- `app/family/**` — create screen (name → show Family Code + a join-QR via `qrcode`), join screen (enter code or scan + **confirm** "You're joining The Tan Family — yes?"), and the Family Love Mix screen (counts + proportional bar + archetype headline; "invite family" prompt when <2 members).
- `components/family/**` — supporting components (code display, join-QR, mix bar).

## Constraints (STRICT)
- Touch ONLY: `app/family/**`, `app/api/family/**`, `components/family/**`. Do NOT modify `lib/*`, `components/ui/*`, `components/led/*`, `app/(experience)/*` (S06 owns result-screen edits), `package.json`, `app/layout.tsx`, `app/globals.css`.
- `bunx tsc --noEmit` green. No full build, no format, no new deps.
- Commit: `git add -A && git commit -m "feat(s05): family create/join + love mix"`. One-line summary.
