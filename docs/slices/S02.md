# Slice S02 — Design-system component kit + gallery

You are an implementation agent in an **isolated git worktree** on branch
`slice/s02-design-kit`. Implement ONLY this slice.

## Read first
- `docs/SPEC.md` → "Design system" section.
- `CONTEXT.md`, `app/globals.css` (tokens + Osmo scaling + `--ease-smooth` already defined), `app/layout.tsx` (fonts), `lib/love-styles.ts`, `lib/cn.ts`, `components/fit-text.tsx` (all exist — build on them).
- The Osmo animation resources are the source of truth for the two GSAP effects. Keep their `data-*` attributes and algorithm intact; only adapt bootstrap to React `useEffect` with cleanup (SplitText.revert / ScrollTrigger.kill / odometer teardown).

## Build (Tailwind v4 + React 19, use `cn` + tokens, `ease-smooth` on transitions)
- `components/ui/`: `button.tsx` (primary + ghost + disabled), `text-link.tsx`, `field.tsx` (labelled input), `progress-bar.tsx`, `quiz-option.tsx` (default/selected + glow), `chip.tsx` (selectable), `checkbox.tsx`, `pill.tsx` (accent-tinted), `love-badge.tsx` (icon frame + name + pill from `LOVE_STYLES`), `section-heading.tsx` (numbered + accent rule).
- `components/animation/`: `split-reveal.tsx` (GSAP SplitText masked line/word/char reveal — the DEFAULT reveal for Geist/Big Shoulders sans headings; `data-split` + `data-split-reveal`), `odometer.tsx` (GSAP number odometer with programmatic `updateOdometer` for live/fluctuating numbers), `traced-script.tsx` (Oooh Baby draw-on via `stroke-dashoffset`).
- **Traced script perf rule:** pre-generate the "Love Revealed" glyph path at BUILD time — write `scripts/gen-traced-hero.ts` using `opentype.js` + `public/fonts/OoohBaby-Regular.ttf`, output `lib/traced-hero.ts` (static `{ d, viewBox }`). `traced-script.tsx` renders that static path. Do NOT load opentype.js or the font on the client.
- `components/led/`: improved LED components (better than the Figma) — `reveal-card.tsx`, `family-constellation.tsx`, `community-dashboard.tsx` (uses odometer), `stat-label.tsx`, `photo-moment.tsx`.
- `app/design-system/page.tsx`: a gallery rendering every token swatch, all primitives, all five love-badges, and live demos of the four animations.
- Replace the starter `app/page.tsx` with a minimal branded landing linking to `/design-system` (traced "Love Revealed" hero).
- If needed, append odometer/split-reveal CSS (+ a FOUC `visibility:hidden` rule for `[data-split]`) to `app/globals.css`, and pin the Big Shoulders family in `app/layout.tsx` to silence the fallback-metrics warning.

## Constraints (STRICT)
- Touch ONLY: `components/**`, `app/design-system/**`, `app/page.tsx`, `app/globals.css`, `app/layout.tsx`, `scripts/gen-traced-hero.ts`, `lib/traced-hero.ts`. Do NOT touch `lib/scoring*`, `lib/repo*`, or `package.json` (gsap, opentype.js, lucide-react, clsx, tailwind-merge already installed).
- Run `bunx tsc --noEmit`. Do NOT run the full production build or format the repo, do NOT add dependencies.
- When green: `git add -A && git commit -m "feat(s02): design-system kit + animations + gallery"` in this worktree.
- Reply with a one-line summary.
