# Slice S09 — Integration + reliability pass

Isolated git worktree on branch `slice/s09-integration`. Implement ONLY this slice.
This is the integrator — it MAY touch cross-cutting wiring files, but keep edits minimal and additive.

## Read first
- `docs/SPEC.md` (Further Notes: reliability, region, accessibility), `docs/ROADMAP.md` (S09), all `docs/adr/*`.
- Existing: `app/lenis-provider.tsx`, `app/layout.tsx`, `components/animation/split-reveal.tsx` (uses GSAP ScrollTrigger), `components/animation/odometer.tsx`, `app/led/*`, `lib/repo/*`.

## Build
- **Lenis ↔ GSAP ScrollTrigger bridge**: in `app/lenis-provider.tsx`, wire `lenis.on('scroll', ScrollTrigger.update)` and drive `lenis.raf` from `gsap.ticker` (`gsap.ticker.add(t => lenis.raf(t*1000)); gsap.ticker.lagSmoothing(0)`), setting Lenis `autoRaf:false`. So scroll-triggered reveals stay in sync with smooth-scroll. Register ScrollTrigger once.
- **prefers-reduced-motion**: ensure SplitReveal, Odometer, TracedScript, transition overlay, shared scene choreography, constellation line draws, and the external LED background all degrade to their final static state under reduced motion (verify/add guards).
- **LED resilience**: confirm `/led` holds last frame + resyncs from cursor on poll failure (add if missing).
- **Env + config**: `.env.example` documenting `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `ADMIN_PIN`. Add `vercel.ts` (or note) pinning region `sin1` if straightforward.
- Add a short `README.md` "Run" section (dev, env, the three surfaces: participant `/`, LED `/led`, admin `/admin`).

## Constraints (STRICT)
- Prefer additive edits. Do NOT rewrite slices' internals; only wire integration. Do NOT touch `package.json` deps (config-only files ok). Do NOT modify `docs/adr/*` or `CONTEXT.md`.
- `bunx tsc --noEmit` green. No full build, no format.
- Commit: `git add -A && git commit -m "feat(s09): integration + reliability"`. One-line summary.
